package com.wildpark.clicon.enums

enum class AvailaibleWidget (val typeItem:String) {
    BOILER("boiler"),
    CLICON("clicon"),
    STATISTIC("statistic"),
    WEATHER("weather"),
    MESSAGE("message")
}
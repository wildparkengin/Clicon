package com.wildpark.clicon.utility

import android.widget.TableLayout
import android.content.Context
import com.wildpark.clicon.entities.Weather


object CreateCustomViewObjects {

    fun createTableWeatherWidget(ctx:Context, tableLayout: TableLayout,weatherData:Weather ){
        val BOOKSHELF_ROWS = 1
        val WEATHER_COLUMNS: Int? = weatherData.featureWeatherData?.size



    }
}